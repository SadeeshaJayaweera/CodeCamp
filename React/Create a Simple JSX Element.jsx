// React is a powerful library for building user interfaces.
// It uses JSX, a syntax extension of JavaScript, to combine HTML-like syntax within JavaScript.
// Below, we replace the <div> element with an <h1> element in JSX.

// Step 1: Define a constant named `JSX`.
// Step 2: Assign an `<h1>` element to the `JSX` constant.
// Step 3: Add the text "Hello JSX!" inside the `<h1>` element.

const JSX = <h1>Hello JSX!</h1>;

// Notes:
// - JSX allows us to write HTML directly within JavaScript.
// - This JSX code will be compiled into JavaScript for React to use.
// - The ReactDOM.render() method will render the JSX into the DOM at the element with id 'root'.

// Example usage:
// ReactDOM.render(JSX, document.getElementById('root'));


// The Solution - 🧑‍💻✅
const JSX = <h1>Hello JSX!</h1>;
